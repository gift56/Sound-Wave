import { Link } from "react-router-dom";

const DetailsHeader = ({ songData, artistId, artistData }) => {
  return <div className="relative w-full flex flex-col"></div>;
};

export default DetailsHeader;
